const style: React.CSSProperties = {
    wrapper: {
        width: '100vw',
        height: '100vh',
        marginTop: -84,
        backgroundImage: 'linear-gradient(to bottom right,#009688, #fff)'
    },
    container: { display: 'flex', justifyContent: 'center', position: 'relative', top: '40%' },
    text: { textAlign: 'center' }
};

export default style;
